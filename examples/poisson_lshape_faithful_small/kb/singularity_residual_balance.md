# Singularity and Residual Balance

Faithful-small L-shaped Poisson runs expose supervised anchors, boundary data,
and residual collocation arrays. Useful mutation ideas include weighting
boundary and residual terms separately, adding polar-coordinate singular basis
features near the re-entrant corner, and sampling extra model capacity around
small radius values.

Keep the evaluator fixed: validation remains prediction-only relative L2 on
private labels.
