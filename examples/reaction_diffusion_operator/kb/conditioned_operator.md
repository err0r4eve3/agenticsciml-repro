# Conditioned Operator

Multiple-input operators often improve when each input field has its own encoder
before fusion. Other useful ideas include normalization per field type,
constraint-conditioned branches, and low-rank decoders over the output grid.
