# Antiderivative Operator Learning Benchmark

Learn an operator that maps a discretized input function `a(s)` to its
antiderivative on the same grid. The model must generalize across unseen input
functions rather than fit a single curve.

This benchmark is a compact proxy for the paper's antiderivative operator
learning task.
