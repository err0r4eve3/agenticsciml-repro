# Cylinder Wake Reconstruction Benchmark

Reconstruct a 2D vorticity field from sparse noisy sensor observations. Inputs
contain four sensor readings and time; outputs are flattened vorticity grids.

This is a compact inverse-problem proxy for the paper's cylinder wake
reconstruction task. It is intended for workflow validation before using large
fluid datasets.
