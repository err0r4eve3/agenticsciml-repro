# Sensor-conditioned Decoder

Sparse fluid-field reconstruction can use a sensor branch to infer latent flow
state and a smooth decoder to produce fields. Useful mutations include temporal
features, low-rank spatial bases, band-limited upsampling, and noise-aware
sensor normalization.
